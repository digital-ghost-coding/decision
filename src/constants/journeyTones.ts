import { colors } from '../theme';
import type { JourneyChapterTone } from '../types/journey';

export const journeyToneColors: Record<JourneyChapterTone, string> = {
  dawn: colors.journeyDawn,
  lavender: colors.journeyLavender,
  sage: colors.journeySage,
  sky: colors.journeySky,
  sunset: colors.journeySunset,
  twilight: colors.journeyTwilight,
};
